version: '3'

services:
  kafka:
    networks:
      - network_ben
    image: docker.io/bitnami/kafka:3.4
    ports:
      - "9092"
    environment:
      - ALLOW_PLAINTEXT_LISTENER=yes
    # depends_on:
      # - sqlserver1

  producer:
    networks:
      - network_ben
    depends_on:
      - kafka
    build:
      context: ./producer
      dockerfile: Dockerfile

  consumer:
    image: docker.io/bitnami/dotnet-sdk:6
    networks:
      - network_ben
    depends_on:
      - producer
    build:
      context: ./consumer
      dockerfile: Dockerfile
    command: dotnet run
    volumes:
      - ./consumer/litedb.sqlite:/appcsharp/litedb.sqlite

  api1:
    image: docker.io/bitnami/dotnet-sdk:6
    networks:
      - network_ben
    # depends_on:
    #   - producer
    build:
      context: ./api1
      dockerfile: Dockerfile
    ports:
      - "7229:7229"
    command: dotnet run
    volumes:
      - ./consumer/litedb.sqlite:/api1/litedb.sqlite

  # sqlserver1:
  #   image: mcr.microsoft.com/mssql/server:2022-latest
  #   networks:
  #     - network_ben
  #   environment:
  #     ACCEPT_EULA: ${DB1_ACCEPT_EULA}
  #     MSSQL_SA_PASSWORD: ${DB1_MSSQL_SA_PASSWORD}
  #     MSSQL_PID: ${DB1_MSSQL_PID}
  #   ports:
  #     - "1433:1433"
  #   volumes:
  #     - ./db/db1:/var/opt/mssql/data

networks:
  network_ben:
    driver: bridge
